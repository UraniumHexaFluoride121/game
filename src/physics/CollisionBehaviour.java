package physics;

public enum CollisionBehaviour {
    //Determines the on-collision behaviour of an object

    PHYSICS, //Calculate physics-based movement on collision
    IMMOVABLE //Solid objects that don't move when colliding
}
